export class Pagination {
    numeroRegistros: number;
    numeroPaginas: number;
    page: number = 1; // current page
    // pageSize: number = 1; // record for page
    // maxSize: number = 6; // number pages max to show in pagination
    // collectionSize: number; // total records
}